# Style Reminders

- Keep strings ASCII (escape quotes) to satisfy lint rules.
- Strong typing: define helper types for worker messages instead of `any`.
- Encourage helper components for repeated button/slider patterns.
