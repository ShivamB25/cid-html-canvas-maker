# Suggested Commands (updated)

- `bun run lint` → verify lint compliance (expect 1 warning in `app/[imageId]/ImageViewer.tsx`).
- `next dev` → run app locally, test upload/generation.
- `next build` → ensure worker bundling succeeds.
