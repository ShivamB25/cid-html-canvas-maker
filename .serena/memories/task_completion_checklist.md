# Task Completion Checklist (updated)

1. Update generator logic/tests.
2. Wire UI state + worker plumbing.
3. Add/expose user controls and exports.
4. Re-run `bun run lint` (expect existing warning in `app/[imageId]/ImageViewer.tsx`).
5. Summarize changes + outstanding warnings in final message.
